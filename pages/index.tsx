import React from 'react'
import styled from 'styled-components'

const Title = styled.h1`
  font-size: 50px;
  color: ${({ theme }) => theme.colors.primary};
`

export const IndexPage = () => <Title>IndexPage</Title>

export default IndexPage
